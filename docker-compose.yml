services:
  ai-career-recommendation:
    build:
      context: .
      dockerfile: Dockerfile
    image: ai-career-recommendation:local
    container_name: ai-career-recommendation
    ports:
      - "8501:8501"
    volumes:
      - ./.env:/app/.env:ro
      - ./data/raw:/app/data/raw:ro
      - ./logs:/app/logs
    restart: unless-stopped
